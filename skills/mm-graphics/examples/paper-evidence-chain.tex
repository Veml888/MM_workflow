\documentclass[border=5pt]{standalone}
\usepackage{fontspec}
\usepackage{tikz}
\usetikzlibrary{positioning,arrows.meta,calc,shapes.geometric}
\setsansfont{Microsoft YaHei}

\definecolor{ink}{HTML}{263238}
\definecolor{line}{HTML}{52616B}
\definecolor{muted}{HTML}{6F7F88}
\definecolor{panel}{HTML}{F5F7F8}
\definecolor{accentpanel}{HTML}{E8F0F2}

\tikzset{
  base/.style={draw=line,line width=0.65pt,text=ink,font=\sffamily\small,
    align=center,inner xsep=10pt,inner ysep=7pt},
  inputbox/.style={base,fill=white,minimum width=3.55cm,minimum height=1.02cm},
  module/.style={base,fill=panel,rounded corners=1.2pt,
    minimum width=4.35cm,minimum height=1.12cm},
  keymodule/.style={module,fill=accentpanel,line width=0.8pt},
  outputbox/.style={base,fill=accentpanel,rounded corners=5pt,
    minimum width=4.35cm,minimum height=1.05cm},
  decision/.style={diamond,base,fill=accentpanel,aspect=2.25,
    inner xsep=8pt,inner ysep=5pt},
  flow/.style={-{Stealth[length=5pt,width=4pt]},draw=line,line width=0.65pt},
  feedback/.style={flow,dashed,draw=muted,rounded corners=3pt},
  edgelabel/.style={font=\sffamily\footnotesize,text=muted,fill=white,inner sep=2pt}
}

\begin{document}
\begin{tikzpicture}
  \node[inputbox] (requirements) {题面要求追踪};
  \node[inputbox,below=0.72cm of requirements] (data) {数据质量审计};

  \coordinate (inputeastmid) at ($(requirements.east)!0.5!(data.east)$);
  \node[keymodule,anchor=west] (contract) at ([xshift=1.35cm]inputeastmid)
    {\textbf{统一问题契约}\\[-1pt]{\footnotesize 变量 · 目标 · 约束}};
  \node[module,below=0.88cm of contract] (model)
    {\textbf{机理优先建模}\\[-1pt]{\footnotesize 候选比较与定型}};
  \node[module,below=0.82cm of model] (solve) {可复现数值求解};
  \node[inputbox,anchor=east] (baseline) at ([xshift=-1.25cm]solve.west)
    {\textbf{基线与扰动方案}\\[-1pt]{\footnotesize 对照 · 极端 · 灵敏度}};
  \node[decision,below=0.90cm of solve] (check) {证据充分？};
  \node[outputbox,below=0.95cm of check] (conclusion) {可信结论与决策建议};
  \node[outputbox,right=1.35cm of conclusion] (support) {图表与复现材料};

  \draw[flow,rounded corners=3pt]
    (requirements.east) -- ++(0.58cm,0) |- ([yshift=8pt]contract.west);
  \draw[flow,rounded corners=3pt]
    (data.east) -- ++(0.58cm,0) |- ([yshift=-8pt]contract.west);
  \draw[flow] (contract) -- (model);
  \draw[flow] (model) -- (solve);
  \draw[flow] (solve) -- (check);
  \draw[flow,rounded corners=3pt]
    (baseline.east) -- ++(0.62cm,0) |- (check.west);
  \draw[flow] (check) -- node[edgelabel,right] {通过} (conclusion);
  \draw[flow] (conclusion) -- (support);
  \draw[feedback] (check.east) -- ++(1.18cm,0)
    |- node[edgelabel,near start,right=2pt] {证据不足} (model.east);
\end{tikzpicture}
\end{document}
