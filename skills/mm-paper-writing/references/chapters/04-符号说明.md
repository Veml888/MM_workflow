# 符号说明

> 本模块由原 `mm-paper-writing/SKILL.md` 的 第150–170行 迁移而来。它是终稿写作的强制规范；不得只读摘要或以其它模块代替。

### 符号说明章节

  只写**一个三线表**（允许跨页，用 `longtable` 按"跨页续表"规则），不配说明段落。列设计为**符号 | 含义 | 单位**；**若大部分符号没有单位，则去掉"单位"列**，只留"符号 | 含义"。**硬性：禁止把符号表做成"两栏并排 / 双列分组"的紧凑布局**；符号表用 `longtable` **就地排版**、不包进 `table`/`figure` 浮动环境，章内**不加 `\FloatBarrier`**；列定义**不得含竖线 `|`**（三线表只允许 toprule/midrule/bottomrule）。表内必须显式使用 `\songti\zihao{5}`，结束后立即恢复 `\songti\zihao{-4}`，避免表格字体或字号泄漏到后续正文。**关于正文引用**：符号表为"说明性/索引性"表格，**章内只放表格本身，不配说明段落、不加引导句**；其 `\label` 无正文 `\ref` 属预期，`check_paper_refs` 已按位置豁免符号说明章内的表格 label（定位方式与 `audit_paper_tables` 的 T-3 一致），不会判孤儿。**硬性红线：机检豁免不等于缺陷，禁止为使符号表满足"每表必引用"而添加引导句、说明段落或正文 `\ref`——那是对本规则的违反，不是修复；"未列出的符号在正文首次出现处说明"属全文执行要求，不得写成本章的引导句。**

**应放进表里的符号**：
1. 跨章节反复出现的核心变量；
2. 目标函数、约束中的关键参数；
3. 向量、矩阵、集合及其下标；
4. 具有特定业务含义或单位的量。

**不应放进表里的符号**：
1. 只出现在个别处的符号变量；
2. 求和号、最大值等常识性算子（如 $\sum$、$\max$、$\min$）；
3. 正文没有使用的预留符号；
4. 为显得复杂而罗列的所有字母。

**判断标准**：删除该符号后，评委是否需要回到正文才能继续理解？需要则保留；不影响理解则不放。
**高频错误（避免）**：同一个符号表示多个含义；符号表与正文不一致；收录过多、临时变量全部写进去；自己造怪符号（非标准、无定义）；隔十页再次出现某符号时不做任何说明。
**表中未列出的符号**：均须在正文**首次出现时**就地说明（定义、单位），不得无提示直接使用。
